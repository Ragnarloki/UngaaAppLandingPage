Vysnap Tech — Ungaa Landing Page

Preview:

1. Open `index.html` in a browser to view the static landing page.
2. Or run a simple static server (e.g., `npx http-server` or `python -m http.server 8000`) and navigate to http://localhost:8000.

This demo contains a client-side simulation of Ungaa's meaning-powered video recommendation. The search input is simulated and uses a lightweight scoring function to suggest videos.
"# UngaaAppLandingPage" 
